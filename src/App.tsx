import "./App.scss";
import Header from "./container/Header";

type Props = {};
const App = (props: Props) => {
  return (
    <div className="app">
      <Header />
    </div>
  );
};

export default App;
